fastapi>=0.104
uvicorn[standard]>=0.24
pydantic>=2.4
pydantic-settings>=2.0
python-dotenv>=1.0
python-multipart>=0.0.9
httpx>=0.27
langchain>=0.2.14
langchain-core>=0.2
langchain-openai>=0.1.22
langchain-text-splitters>=0.3
llama-index-core>=0.12
langgraph>=0.2.6
langfuse>=4.0,<5.0
loguru>=0.7
opencc-python-reimplemented>=0.1.7
asyncpg>=0.29
pymilvus>=2.5,<3
openai>=1.0
numpy>=1.26
PyMuPDF>=1.23
# Phase-2 multimodal asset store (MULTIMODAL_ASSET_STORE=minio; local store needs none of this)
minio>=7.2
pdfplumber>=0.10
python-docx>=1.1
chardet>=5.2
# ragas 0.4.3 (PyPI latest): the old git-commit pin (PR #2769, lazy VertexAI
# imports) is unfetchable — the commit lives on an unmerged contributor fork.
# The eager-import crash is handled at our import boundary instead:
# src/agent/tools/ragas_compat.py stubs the removed vertexai module.
ragas>=0.4.3
# pyarrow 24 crashes natively (access violation) on Windows/Py3.13 inside this stack;
# 18.1 lacks pyarrow.json_ used by the datasets chain — 21.0.0 is the verified window.
pyarrow==21.0.0
# CUDA torch: PyPI Windows wheels are CPU-only and would silently replace GPU
# inference, and the former file:///C:/... local-wheel pin broke every CI run
# (Linux runners can't resolve Windows paths). Non-Windows (CI) installs from
# PyPI; Windows keeps the skill-installed local CUDA wheel untouched (the
# installed version satisfies the spec, so nothing is ever replaced).
torch>=2.12.1; sys_platform != "win32"
torchvision>=0.27.1; sys_platform != "win32"
sentence-transformers>=4.1.0
transformers>=4.51.0
bitsandbytes>=0.46.0
accelerate>=1.6.0

# Optional: EDGAR HTML partitioning (standalone script scripts/run_unstructured_edgar_html.py)
unstructured[html]>=0.16.0
