# 企业级 5 万+ 文档 RAG 设计参考

基于实战经验总结的企业级 RAG 系统设计方案，适用于制药、金融等高严谨行业的海量异构文档场景。

## 核心约束

- **数据规模**：5 万+ 异构文档，单文件最高 200 页，文档间大量交叉引用
- **格式异构**：PDF / Word / 扫描件 / CSV 电子表格多格式混合
- **容错零容忍**：检索漏信息会造成百万级业务损失，不能只满足"大部分有效"
- **行业合规**：数据主权、敏感数据不出内网、对幻觉和稳定性要求极高

## 文档处理：四层层级分块

放弃传统单一固定长度分块，采用四层递进层级组块：

| 层级 | 定位 | 内容 |
|------|------|------|
| 文档层（顶层） | 全局元数据 | 论文标题、作者、出版日期、文档类型 |
| 章节层 | 内容逻辑边界 | 摘要/方法/结果/讨论，保留学术/行业文档天然结构 |
| 段落块层 | 标准检索单元 | 200-400 tokens，重叠 ≤ 50 tokens，平衡上下文完整性与检索粒度 |
| 句子层（底层） | 最小精准检索单元 | 定位原文精确证据，杜绝幻觉 |

## 元数据体系（投入 40% 研发资源）

每一个层级块绑定统一元数据字段，支撑混合检索、过滤、关联查询：

- **基础属性**：文档类型、章节类型、块层级、父子层级关联 ID（用于图检索）
- **领域标签**：提取的领域特定关键词、监管分类（FDA/EMA/ICH）
- **预计算指标**：预打分相关性评分

元数据作用是实现**规则过滤 + 语义检索联动**，是企业级混合检索的基础。无论词嵌入做得多好，糟糕的元数据都会导致糟糕的检索效果。

## 检索架构：向量 + 图双层

### 基础层：传统向量语义检索
基于段落块向量匹配用户问题，完成粗召回。

### 上层增强：图检索层
利用元数据中的父子关系、文档交叉引用构建知识图，解决文档之间依赖和引用关联问题，避免只检索单份文档、丢失关联佐证信息。

### 整体模式：混合检索（拒绝纯语义）
多模块协同：**关键词规则检索 + 向量语义召回 + 重排序器 + 文档摘要过滤 + 标签筛选**，多层过滤提升精准度。

## 模型选型策略

生产环境优先选择开源可本地部署的模型（如 Qwen QWQ-32B）：

| 维度 | 理由 |
|------|------|
| 成本 | 大批量处理相比 GPT-4o 降低约 85% |
| 数据主权 | 本地自托管，敏感数据不出内网 |
| 微调能力 | 可基于行业私有文档微调，适配专业术语 |
| 延迟 | 私有化部署，响应时间稳定，无第三方限流 |

GPT-4o 在专业领域（如医药问答）容易生成不存在的药物相互作用（幻觉严重），领域微调后的开源模型在专业术语理解和事实准确性上大幅提升。

## 多格式兼容

对非纯文本文档增加多模态解析链路：

- 传统文本 RAG 流程 + 计算机视觉模块组合
- 为 PDF / Word / 扫描图 / 表格分别开发自定义解析流水线
- 从财务图表中提取结构化数据，避免表格信息丢失

## 生产落地四大准则

1. **元数据优先**：40% 研发资源投入元数据设计，元数据质量直接决定检索上限，向量嵌入无法弥补元数据缺陷
2. **拒绝纯语义检索**：企业场景必须混合规则、关键词、重排、标签多模块协同
3. **领域微调是高回报投资**：医疗、法律、金融专业领域必须做私有数据微调，显著降低幻觉
4. **基础设施不可妥协**：完善监控、容灾备份、SLA 可用性保障，企业客户愿意为稳定付费

## 业务收益参考

- 制药合规助手：5 万+ 论文/FDA 指南，自动合规审查，监管问询响应速度提升 90%
- 银行并购尽调：多模态财务文档处理，尽职调查周期缩短 75%
- 规范落地的企业 RAG，6 个月可实现 10 倍以上投资回报

## 与本项目的差距（待改进项）

对比 RAGAS-FINANCE 当前实现：

1. **图检索层缺失** — 仅有 section tree 的 parent_id 层级关系，无跨文档引用图和无图遍历检索
2. **无句子级精准检索** — 最小检索单元是段落块（1500 字符），缺少 sentence-level retrieval
3. **无多模态能力** — 无扫描件 OCR、无图表 CV、无 CSV 专项解析
4. **无本地模型部署/微调** — 所有模型走云端 API
5. **领域不同** — 当前是 SEC 财务披露，需扩展到制药/医疗等领域的元数据标签体系
